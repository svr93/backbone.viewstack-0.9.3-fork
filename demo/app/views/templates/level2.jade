.view-head.nav-bar
  a.nav-bar-button-left(href="#level1") Back
  
  .nav-bar-title.view-body Next Level

  a.nav-bar-button-right(href="#level3") More

.view-body: .content
  p This view is automatically pushed onto the view. You can manually swipe back to the last view.

  p Because this view declares a stack, the stack is created on initialization. This means you can refresh the page and still manually swipe back.

  pre: code stack: ["level1", "level2"]

  a.button(href="#fade") Fade to popover

  a.button(href="#zoom") Zoom to popover
  
